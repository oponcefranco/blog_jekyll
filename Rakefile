# namespace :assets do
#   task :precompile do
#     puts `bundle exec jekyll build`
#   end
# end

namespace :assets do
  desc 'Precompile assets'
  task :precompile do
    sh "bundle exec jekyll build"
  end
end